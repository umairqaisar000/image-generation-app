{
    "rules": {
        ".read": false,
        ".write": false,
        "userMessages": {
            "$uid": {
                ".read": "$uid === auth.uid",
                ".write": "$uid === auth.uid"
            }
        }
    }
}